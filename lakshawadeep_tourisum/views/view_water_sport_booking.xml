<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- SEARCH VIEW -->

    <record id="view_water_sport_booking_search" model="ir.ui.view">
        <field name="name">tour.water.sport.booking.search</field>
        <field name="model">tour.water.sport.booking</field>
        <field name="arch" type="xml">
            <search>

                <field name="water_sport_id"/>
                <field name="customer_id"/>

                <filter name="draft"
                        string="Draft"
                        domain="[('state','=','draft')]"/>

                <filter name="confirmed"
                        string="Confirmed"
                        domain="[('state','=','confirmed')]"/>

                <filter name="cancelled"
                        string="Cancelled"
                        domain="[('state','=','cancelled')]"/>

                <group expand="0" string="Group By">

                    <filter name="group_sport"
                            string="Water Sport"
                            context="{'group_by':'water_sport_id'}"/>

                    <filter name="group_customer"
                            string="Customer"
                            context="{'group_by':'customer_id'}"/>

                    <filter name="group_state"
                            string="Status"
                            context="{'group_by':'state'}"/>

                </group>

            </search>
        </field>
    </record>

    <!-- LIST VIEW -->

    <record id="view_water_sport_booking_list" model="ir.ui.view">
        <field name="name">tour.water.sport.booking.list</field>
        <field name="model">tour.water.sport.booking</field>
        <field name="arch" type="xml">
            <list>

                <field name="booking_datetime"/>

                <field name="water_sport_id"/>

                <field name="customer_id"/>

                <field name="booking_date"/>

                <field name="participants"/>

                <field name="state"/>

            </list>
        </field>
    </record>

    <!-- FORM VIEW -->

    <record id="view_water_sport_booking_form" model="ir.ui.view">
        <field name="name">tour.water.sport.booking.form</field>
        <field name="model">tour.water.sport.booking</field>
        <field name="arch" type="xml">

            <form>

                <header>

                    <field name="state"
                           widget="statusbar"
                           statusbar_visible="draft,confirmed,cancelled"/>

                </header>

                <sheet>

                    <group>

                        <group>

                            <field name="water_sport_id"/>

                            <field name="customer_id"/>

                            <field name="booking_datetime"
                                   readonly="1"/>

                        </group>

                        <group>

                            <field name="booking_date"/>

                            <field name="participants"/>

                        </group>

                    </group>

                    <group>

                        <field name="description"/>

                    </group>

                </sheet>

            </form>

        </field>
    </record>

    <!-- ACTION -->

    <record id="action_water_sport_booking"
            model="ir.actions.act_window">

        <field name="name">Water Sport Bookings</field>

        <field name="res_model">tour.water.sport.booking</field>

        <field name="view_mode">list,form</field>

        <field name="search_view_id"
               ref="view_water_sport_booking_search"/>

    </record>

    <!-- MENU -->

    <menuitem id="menu_water_sport_booking"
              name="Bookings"
              parent="menu_water_sport"
              action="action_water_sport_booking"
              sequence="40"/>

</odoo>